<local:AllViewBase x:Class="MVVMFirma.Views.AllProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"/>
                <DataGridTextColumn Header="Nazwa"/>
                <DataGridTextColumn Header="Opis"/>
                <DataGridTextColumn Header="Kategoria"/>
                <DataGridTextColumn Header="Cena zakupu"/>
                <DataGridTextColumn Header="Cena sprzedazy"/>
                <DataGridTextColumn Header="Data waznosci"/>
                <DataGridTextColumn Header="Producent"/>
                <DataGridTextColumn Header="Na recepte"/>
                <DataGridTextColumn Header="Refundacja"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
