<local:OneViewBase x:Class="MVVMFirma.Views.NewPrescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400">

    <Grid Background="#fff">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Dodaj nową receptę" 
                   HorizontalAlignment="Left" VerticalAlignment="Center" 
                   FontSize="20" FontWeight="Bold" Margin="5,10,0,10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Pacjent:" Margin="0,8,0,8"/>
            <ComboBox Grid.Column="1" Grid.Row="0" 
                      ItemsSource="{Binding Pacjenci}" 
          SelectedValue="{Binding IDPacjenta, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                      DisplayMemberPath="PESEL" 
                      SelectedValuePath="ID_Pacjenta" Margin="0,8,0,8"/>

            <Label Grid.Column="0" Grid.Row="1" Content="Farmaceuta:" Margin="0,8,0,8"/>
            <ComboBox Grid.Column="1" Grid.Row="1" 
                      ItemsSource="{Binding Farmaceuci}" 
          SelectedValue="{Binding IDFarmaceuty, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                      DisplayMemberPath="Numer_Licencji" 
                      SelectedValuePath="ID_Farmaceuty" Margin="0,8,0,8"/>

            <Label Grid.Column="0" Grid.Row="2" Content="Data wystawienia:" Margin="0,8,0,8"/>
            <DatePicker Grid.Column="1" Grid.Row="2" 
            SelectedDate="{Binding DataWystawienia, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0,8,0,8"/>

            <Label Grid.Column="0" Grid.Row="3" Content="Data realizacji:" Margin="0,8,0,8"/>
            <DatePicker Grid.Column="1" Grid.Row="3" 
            SelectedDate="{Binding DataRealizacji, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0,8,0,8"/>
        </Grid>
    </Grid>
</local:OneViewBase>
