<local:OneViewBase x:Class="MVVMFirma.Views.NewParagonView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:MVVMFirma.Views"
                   mc:Ignorable="d"
                   d:DesignHeight="500"
                   d:DesignWidth="400">

    <Grid Background="#fff">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <TextBlock Grid.Row="0" 
                   Text="Dodaj nowy paragon"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="5,10,0,10"/>

        <!-- Formularz -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="45"/>
            </Grid.RowDefinitions>

            <!-- Numer paragonu -->
            <Label Grid.Row="0" Grid.Column="0" Margin="0,8,0,8"
                   Content="Numer paragonu:"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,8,0,8" 
                     Text="{Binding NumerParagonu, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <!-- Sprzedaż (zamiast ComboBox => text + button) -->
            <Label Grid.Row="1" Grid.Column="0" Margin="0,8,0,8"
                   Content="Sprzedaż:"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,8,0,8" 
                     IsEnabled="False"
                     Text="{Binding SaleInfo, Mode=TwoWay}"/>
            <Button Grid.Row="1" Grid.Column="2" Margin="5,8,0,8"
                    Width="50"
                    Content="Wybierz"
                    Command="{Binding ShowSales}"/>

            <!-- Data wystawienia -->
            <Label Grid.Row="2" Grid.Column="0" Margin="0,8,0,8"
                   Content="Data wystawienia:"/>
            <DatePicker Grid.Row="2" Grid.Column="1" Margin="0,8,0,8"
                        SelectedDate="{Binding DataWystawienia, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <!-- Kwota -->
            <Label Grid.Row="3" Grid.Column="0" Margin="0,8,0,8"
                   Content="Kwota:"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="0,8,0,8"
                     Text="{Binding Kwota, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
        </Grid>
    </Grid>
</local:OneViewBase>
